<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <%= label f, :date %>
  <%= date_select f, :date %>
  <%= error_tag f, :date %>

  <%= label f, :agenda_url %>
  <%= text_input f, :agenda_url %>
  <%= error_tag f, :agenda_url %>

  <%= label f, :minutes_url %>
  <%= text_input f, :minutes_url %>
  <%= error_tag f, :minutes_url %>

  <%= label f, :body %>
  <%= text_input f, :body %>
  <%= error_tag f, :body %>

  <div>
    <%= submit "Save" %>
  </div>
<% end %>
