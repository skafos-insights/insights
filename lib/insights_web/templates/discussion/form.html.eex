<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <%= label f, :present %>
  <%= text_input f, :present %>
  <%= error_tag f, :present %>

  <%= label f, :absent %>
  <%= text_input f, :absent %>
  <%= error_tag f, :absent %>

  <div>
    <%= submit "Save" %>
  </div>
<% end %>
